[build]
  base = "frontend"                    # ← This is the most important line
  command = "npm ci && npm run build"   # or "ng build --configuration production"
  publish = "dist/frontend"            # ← Change "converter" to your actual output folder name

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200