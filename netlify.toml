[build]
  command = "npm run build --prefix frontend"
  publish = "frontend/dist/converter"     # ← Change "converter" to your actual Angular output folder name

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200